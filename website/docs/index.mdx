---
sidebar_position: 1
---

# DAMIAOモーター

DAMIAO モーター - 高精度・高安定性のロボットジョイント用モーター。

```mdx-code-block
import DocCardList from '@theme/DocCardList';
import {useDocById} from '@docusaurus/plugin-content-docs/client';

export const HardwareList = () => {
  const hardwareDocs = [
    useDocById('products/hardware/dm-j4310-2ec-v1.1'),
    useDocById('products/hardware/dm-j4340-2ec-v1.0'),
    useDocById('products/hardware/dm-j4340p-2ec-v1.0'),
    useDocById('products/hardware/dm-j8009p-2ec-v1.0'),
    useDocById('products/hardware/dm-usbcu1'),
  ];
  const items = hardwareDocs.map((doc) => {
    return {
      type: 'link',
      label: doc.title,
      href: doc.id,
      docId: doc.id,
      description: doc.description,
    };
  })
  return <DocCardList items={items} />;
};

<HardwareList />
```
